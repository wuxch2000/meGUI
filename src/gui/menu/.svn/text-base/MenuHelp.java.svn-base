package gui.menu;

import config.properties.*;

@SuppressWarnings("serial")
public class MenuHelp extends A_Menu {

    public MenuHelp() {
        super(DialogProperties.Key.Help);
        add(new BrowseLogAction());
        add(new OpenTutorialDialogAction());
        add(new AboutAction());
    }
}
